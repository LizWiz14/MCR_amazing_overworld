# ---
# Definitions
# ---
#
# averageCountPerChunk
#  : On average many deposits appear within a single chunk / 16x16 grid of columns.
#
# salt
#  : Adjusts the random seed for the placement of each deposit.
#
#  Every deposit should have a unique salt value, such that they all have unique distributions.
#
# range
#  : Specifies the height range of deposits on the Y axis.
#

# To adjust the size of individual deposits, refer to the relevant
# configs found in the /structures/deposits/ directory.

# ---
# Ore Veins
# ---
# The height ranges and count per chunk are defined to roughly match that of vanilla 1.18.

# Ores prefixed with '-uniform' are uniformly distributed (Equal chance of generating across the specified Y range), otherwise
# ores are normally distributed such that they are more common closer to the halfway point between the min and max.

# For math nerds: Standard deviation is calculated via meta configuration based on min & max such that the majority (~99.7%)
# of random heights calculated fit within said min & max. (This is calculated by simply dividing the distance between min and max by 6.)
# This behaviour is configured within each individual ore FEATURE config.

lapis:
  averageCountPerChunk: 2
  salt: 3542
  range:
    min: ${meta.yml:bottom-y} + 32                            #MIRABILIS: change height to automaticly adjust based on min height
    max: ${customization.yml:terrain-base-y-level} - 64       #MIRABILIS: change height to automaticly adjust based on default height (193)

lapis-uniform:
  averageCountPerChunk: 4
  salt: 5329
  range:
    min: ${meta.yml:bottom-y}                                 #MIRABILIS: change height to automaticly adjust
    max: ${customization.yml:terrain-base-y-level} - 1        #MIRABILIS: change height to automaticly adjust

diamond:
  averageCountPerChunk: 6
  salt: 1032
  range:
    min: ${meta.yml:bottom-y} - 80                            #MIRABILIS: change height to automaticly adjust
    max: ${customization.yml:terrain-base-y-level} - 144      #MIRABILIS: change height to automaticly adjust

iron:
  averageCountPerChunk: 10
  salt: 6921
  range:
    min: ${meta.yml:bottom-y} + 40                            #MIRABILIS: change height to automaticly adjust
    max: ${customization.yml:terrain-base-y-level} - 8        #MIRABILIS: change height to automaticly adjust

iron-uniform:
  averageCountPerChunk: 10
  salt: 2013
  range:
    min: ${meta.yml:bottom-y}                                 #MIRABILIS: change height to automaticly adjust
    max: ${customization.yml:terrain-base-y-level} + 8        #MIRABILIS: change height to automaticly adjust

iron-high:
  averageCountPerChunk: 90
  salt: 2031
  range:
    min: ${meta.yml:bottom-y} + 144                           #MIRABILIS: change height to automaticly adjust
    max: ${customization.yml:terrain-base-y-level} + 64       #MIRABILIS: change height to automaticly adjust

copper:
  averageCountPerChunk: 16
  salt: 2932
  range:
    min: ${meta.yml:bottom-y} + 48                            #MIRABILIS: change height to automaticly adjust
    max: ${customization.yml:terrain-base-y-level} - 20       #MIRABILIS: change height to automaticly adjust

redstone:
  averageCountPerChunk: 8
  salt: 7019
  range:
    min: ${meta.yml:bottom-y} - 32                            #MIRABILIS: change height to automaticly adjust
    max: ${meta.yml:bottom-y} + 32                            #MIRABILIS: change height to automaticly adjust

redstone-uniform:
  averageCountPerChunk: 4
  salt: 1923
  range:
    min: ${meta.yml:bottom-y}                                 #MIRABILIS: change height to automaticly adjust
    max: ${customization.yml:terrain-base-y-level} - 48       #MIRABILIS: change height to automaticly adjust

coal:
  averageCountPerChunk: 10
  salt: 1068
  range:
    min: ${meta.yml:bottom-y} + 64                            #MIRABILIS: change height to automaticly adjust
    max: ${customization.yml:terrain-base-y-level} + 128      #MIRABILIS: change height to automaticly adjust

coal-uniform:
  averageCountPerChunk: 15
  salt: 7012
  range:
    min: ${meta.yml:bottom-y} + 200                           #MIRABILIS: change height to automaticly adjust
    max: ${meta.yml:top-y} + 64                               #MIRABILIS: change height to automaticly adjust

gold:
  averageCountPerChunk: 4
  salt: 5912
  range:
    min: ${meta.yml:bottom-y}                                 #MIRABILIS: change height to automaticly adjust
    max: ${customization.yml:terrain-base-y-level} - 48       #MIRABILIS: change height to automaticly adjust

gold-uniform:
  averageCountPerChunk: 1/2
  salt: 3313
  range:
    min: ${meta.yml:bottom-y}                                 #MIRABILIS: change height to automaticly adjust
    max: ${meta.yml:bottom-y} + 16                            #MIRABILIS: change height to automaticly adjust

gold-high:
  averageCountPerChunk: 4
  salt: 7462
  range:
    min: ${customization.yml:terrain-base-y-level} + 32       #MIRABILIS: change height to automaticly adjust
    max: ${meta.yml:top-y} + 64                               #MIRABILIS: change height to automaticly adjust

emerald:
  averageCountPerChunk: 100
  salt: 5091
  range:
    min: ${meta.yml:bottom-y} + 48                            #MIRABILIS: change height to automaticly adjust
    max: ${meta.yml:top-y} + 64                               #MIRABILIS: change height to automaticly adjust

# ---
# Deposits
# ---
# All deposits are uniformly distributed, unlike ore veins.

granite:
  averageCountPerChunk: 2
  salt: 9141
  range:
    min: ${meta.yml:bottom-y} + 64                            #MIRABILIS: change height to automaticly adjust
    max: ${meta.yml:top-y} - 64                               #MIRABILIS: change height to automaticly adjust

andesite:
  averageCountPerChunk: 2
  salt: 1923
  range:
    min: ${meta.yml:bottom-y} + 64                            #MIRABILIS: change height to automaticly adjust
    max: ${meta.yml:top-y} - 64                               #MIRABILIS: change height to automaticly adjust

dirt:
  averageCountPerChunk: 2
  salt: 2139
  range:
    min: ${meta.yml:bottom-y} + 64                            #MIRABILIS: change height to automaticly adjust
    max: ${meta.yml:top-y} - 64                               #MIRABILIS: change height to automaticly adjust

diorite:
  averageCountPerChunk: 2
  salt: 6912
  range:
    min: ${meta.yml:bottom-y} + 64                            #MIRABILIS: change height to automaticly adjust
    max: ${meta.yml:top-y} - 64                               #MIRABILIS: change height to automaticly adjust

clay:
  averageCountPerChunk: 2
  salt: 1785
  range:
    min: ${meta.yml:bottom-y} + 114                           #MIRABILIS: change height to automaticly adjust
    max: ${meta.yml:top-y} - 64                               #MIRABILIS: change height to automaticly adjust

gravel:
  averageCountPerChunk: 4
  salt: 1239
  range:
    min: ${meta.yml:bottom-y} + 32                            #MIRABILIS: change height to automaticly adjust
    max: ${meta.yml:top-y} - 64                               #MIRABILIS: change height to automaticly adjust

tuff:
  averageCountPerChunk: 2
  salt: 1239
  range:
    min: ${meta.yml:bottom-y}                                 #MIRABILIS: change height to automaticly adjust
    max: ${customization.yml:terrain-base-y-level} - 48       #MIRABILIS: change height to automaticly adjust

river-dripstone:
  averageCountPerChunk: 2
  salt: 2952
  range:
    min: ${meta.yml:bottom-y} + 139                           #MIRABILIS: change height to automaticly adjust
    max: ${customization.yml:terrain-base-y-level} + 20       #MIRABILIS: change height to automaticly adjust

powdersnow:
  averageCountPerChunk: 1
  salt: 5825
  range:
    min: ${customization.yml:terrain-base-y-level} - 20       #MIRABILIS: change height to automaticly adjust
    max: ${meta.yml:top-y} + 64                               #MIRABILIS: change height to automaticly adjust

blueice:
  averageCountPerChunk: 7
  salt: 1678
  range:
    min: ${customization.yml:terrain-base-y-level} - 20       #MIRABILIS: change height to automaticly adjust
    max: ${customization.yml:terrain-base-y-level} + 1        #MIRABILIS: change height to automaticly adjust
